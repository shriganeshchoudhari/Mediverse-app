global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerting_rules.yml"
  - "recording_rules.yml"

scrape_configs:
  - job_name: "mediverse-backend"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["backend:8085"]
